-- ── Clipboard ──────────────────────────────────────────
-- Generated by hyprlang2lua. Review TODOs before reloading Hyprland.

-- TODO: manual review on line 2 — no mapping for dispatcher "Universal copy"
-- hl.bind("SUPER + C", hl.dsp.Universal_copy("sendshortcut", "CTRL", "Insert", ""), { drag = true })
-- TODO: manual review on line 3 — no mapping for dispatcher "Universal paste"
-- hl.bind("SUPER + V", hl.dsp.Universal_paste("sendshortcut", "SHIFT", "Insert", ""), { drag = true })
-- TODO: manual review on line 4 — no mapping for dispatcher "Universal cut"
-- hl.bind("SUPER + X", hl.dsp.Universal_cut("sendshortcut", "CTRL", "X", ""), { drag = true })
-- TODO: manual review on line 5 — no mapping for dispatcher "Clipboard manager"
-- hl.bind("SUPER + CTRL + V", hl.dsp.Clipboard_manager("exec", "walker -m clipboard"), { drag = true })

-- ── Volume / Brightness ────────────────────────────────
local osdclient = "swayosd-client --monitor \"$(hyprctl monitors -j | jq -r '.[] | select(.focused == true).name')\""

-- TODO: manual review on line 10 — no mapping for dispatcher "Volume up"
-- hl.bind("XF86AudioRaiseVolume", hl.dsp.Volume_up("exec", osdclient .. " --output-volume raise"), { locked = true, repeating = true, drag = true })
-- TODO: manual review on line 11 — no mapping for dispatcher "Volume down"
-- hl.bind("XF86AudioLowerVolume", hl.dsp.Volume_down("exec", osdclient .. " --output-volume lower"), { locked = true, repeating = true, drag = true })
-- TODO: manual review on line 12 — no mapping for dispatcher "Mute"
-- hl.bind("XF86AudioMute", hl.dsp.Mute("exec", osdclient .. " --output-volume mute-toggle"), { locked = true, repeating = true, drag = true })
-- TODO: manual review on line 13 — no mapping for dispatcher "Mute microphone"
-- hl.bind("XF86AudioMicMute", hl.dsp.Mute_microphone("exec", osdclient .. " --input-volume mute-toggle"), { locked = true, repeating = true, drag = true })
-- TODO: manual review on line 14 — no mapping for dispatcher "Brightness up"
-- hl.bind("XF86MonBrightnessUp", hl.dsp.Brightness_up("exec", osdclient .. " --brightness raise"), { locked = true, repeating = true, drag = true })
-- TODO: manual review on line 15 — no mapping for dispatcher "Brightness down"
-- hl.bind("XF86MonBrightnessDown", hl.dsp.Brightness_down("exec", osdclient .. " --brightness lower"), { locked = true, repeating = true, drag = true })

-- TODO: manual review on line 17 — no mapping for dispatcher "Volume up precise"
-- hl.bind("ALT + XF86AudioRaiseVolume", hl.dsp.Volume_up_precise("exec", osdclient .. " --output-volume +1"), { locked = true, repeating = true, drag = true })
-- TODO: manual review on line 18 — no mapping for dispatcher "Volume down precise"
-- hl.bind("ALT + XF86AudioLowerVolume", hl.dsp.Volume_down_precise("exec", osdclient .. " --output-volume -1"), { locked = true, repeating = true, drag = true })
-- TODO: manual review on line 19 — no mapping for dispatcher "Brightness up precise"
-- hl.bind("ALT + XF86MonBrightnessUp", hl.dsp.Brightness_up_precise("exec", osdclient .. " --brightness +1"), { locked = true, repeating = true, drag = true })
-- TODO: manual review on line 20 — no mapping for dispatcher "Brightness down precise"
-- hl.bind("ALT + XF86MonBrightnessDown", hl.dsp.Brightness_down_precise("exec", osdclient .. " --brightness -1"), { locked = true, repeating = true, drag = true })

-- TODO: manual review on line 22 — no mapping for dispatcher "Next track"
-- hl.bind("XF86AudioNext", hl.dsp.Next_track("exec", osdclient .. " --playerctl next"), { locked = true, drag = true })
-- TODO: manual review on line 23 — no mapping for dispatcher "Pause"
-- hl.bind("XF86AudioPause", hl.dsp.Pause("exec", osdclient .. " --playerctl play-pause"), { locked = true, drag = true })
-- TODO: manual review on line 24 — no mapping for dispatcher "Play"
-- hl.bind("XF86AudioPlay", hl.dsp.Play("exec", osdclient .. " --playerctl play-pause"), { locked = true, drag = true })
-- TODO: manual review on line 25 — no mapping for dispatcher "Previous track"
-- hl.bind("XF86AudioPrev", hl.dsp.Previous_track("exec", osdclient .. " --playerctl previous"), { locked = true, drag = true })

-- TODO: manual review on line 27 — no mapping for dispatcher "Switch audio output"
-- hl.bind("SUPER + XF86AudioMute", hl.dsp.Switch_audio_output("exec", "wpctl set-default $(pactl list short sinks | grep -v \"RUNNING\" | head -1 | awk '{print $1}') && notify-send \"Audio output switched\""), { locked = true, drag = true })

-- ── Close ──────────────────────────────────────────────
-- TODO: manual review on line 30 — no mapping for dispatcher "Close window"
-- hl.bind("SUPER + W", hl.dsp.Close_window("killactive", ""), { drag = true })
-- TODO: manual review on line 31 — no mapping for dispatcher "Close all windows"
-- hl.bind("CTRL + ALT + DELETE", hl.dsp.Close_all_windows("exec", "close-all-windows"), { drag = true })

-- ── Window Management ──────────────────────────────────
-- TODO: manual review on line 34 — no mapping for dispatcher "Toggle window split"
-- hl.bind("SUPER + J", hl.dsp.Toggle_window_split("togglesplit", ""), { drag = true })
-- TODO: manual review on line 35 — no mapping for dispatcher "Pseudo window"
-- hl.bind("SUPER + P", hl.dsp.Pseudo_window("pseudo", ""), { drag = true })
-- TODO: manual review on line 36 — no mapping for dispatcher "Toggle window floating/tiling"
-- hl.bind("SUPER + T", hl.dsp.Toggle_window_floating_tiling("togglefloating", ""), { drag = true })
-- TODO: manual review on line 37 — no mapping for dispatcher "Full screen"
-- hl.bind("SUPER + F", hl.dsp.Full_screen("fullscreen", 0), { drag = true })
-- TODO: manual review on line 38 — no mapping for dispatcher "Tiled full screen"
-- hl.bind("SUPER + CTRL + F", hl.dsp.Tiled_full_screen("fullscreenstate", "0 2"), { drag = true })
-- TODO: manual review on line 39 — no mapping for dispatcher "Full width"
-- hl.bind("SUPER + ALT + F", hl.dsp.Full_width("fullscreen", 1), { drag = true })
-- TODO: manual review on line 40 — no mapping for dispatcher "Pop window out"
-- hl.bind("SUPER + O", hl.dsp.Pop_window_out("exec", "hyprctl dispatch togglefloating && hyprctl dispatch pin"), { drag = true })

-- ── Focus ──────────────────────────────────────────────
-- TODO: manual review on line 43 — no mapping for dispatcher "Move window focus left"
-- hl.bind("SUPER + LEFT", hl.dsp.Move_window_focus_left("movefocus", "l"), { drag = true })
-- TODO: manual review on line 44 — no mapping for dispatcher "Move window focus right"
-- hl.bind("SUPER + RIGHT", hl.dsp.Move_window_focus_right("movefocus", "r"), { drag = true })
-- TODO: manual review on line 45 — no mapping for dispatcher "Move window focus up"
-- hl.bind("SUPER + UP", hl.dsp.Move_window_focus_up("movefocus", "u"), { drag = true })
-- TODO: manual review on line 46 — no mapping for dispatcher "Move window focus down"
-- hl.bind("SUPER + DOWN", hl.dsp.Move_window_focus_down("movefocus", "d"), { drag = true })

-- ── Workspaces ─────────────────────────────────────────
-- TODO: manual review on line 49 — no mapping for dispatcher "Switch to workspace 1"
-- hl.bind("SUPER + code:10", hl.dsp.Switch_to_workspace_1("workspace", 1), { drag = true })
-- TODO: manual review on line 50 — no mapping for dispatcher "Switch to workspace 2"
-- hl.bind("SUPER + code:11", hl.dsp.Switch_to_workspace_2("workspace", 2), { drag = true })
-- TODO: manual review on line 51 — no mapping for dispatcher "Switch to workspace 3"
-- hl.bind("SUPER + code:12", hl.dsp.Switch_to_workspace_3("workspace", 3), { drag = true })
-- TODO: manual review on line 52 — no mapping for dispatcher "Switch to workspace 4"
-- hl.bind("SUPER + code:13", hl.dsp.Switch_to_workspace_4("workspace", 4), { drag = true })
-- TODO: manual review on line 53 — no mapping for dispatcher "Switch to workspace 5"
-- hl.bind("SUPER + code:14", hl.dsp.Switch_to_workspace_5("workspace", 5), { drag = true })
-- TODO: manual review on line 54 — no mapping for dispatcher "Switch to workspace 6"
-- hl.bind("SUPER + code:15", hl.dsp.Switch_to_workspace_6("workspace", 6), { drag = true })
-- TODO: manual review on line 55 — no mapping for dispatcher "Switch to workspace 7"
-- hl.bind("SUPER + code:16", hl.dsp.Switch_to_workspace_7("workspace", 7), { drag = true })
-- TODO: manual review on line 56 — no mapping for dispatcher "Switch to workspace 8"
-- hl.bind("SUPER + code:17", hl.dsp.Switch_to_workspace_8("workspace", 8), { drag = true })
-- TODO: manual review on line 57 — no mapping for dispatcher "Switch to workspace 9"
-- hl.bind("SUPER + code:18", hl.dsp.Switch_to_workspace_9("workspace", 9), { drag = true })
-- TODO: manual review on line 58 — no mapping for dispatcher "Switch to workspace 10"
-- hl.bind("SUPER + code:19", hl.dsp.Switch_to_workspace_10("workspace", 10), { drag = true })

-- TODO: manual review on line 60 — no mapping for dispatcher "Move window to workspace 1"
-- hl.bind("SUPER + SHIFT + code:10", hl.dsp.Move_window_to_workspace_1("movetoworkspace", 1), { drag = true })
-- TODO: manual review on line 61 — no mapping for dispatcher "Move window to workspace 2"
-- hl.bind("SUPER + SHIFT + code:11", hl.dsp.Move_window_to_workspace_2("movetoworkspace", 2), { drag = true })
-- TODO: manual review on line 62 — no mapping for dispatcher "Move window to workspace 3"
-- hl.bind("SUPER + SHIFT + code:12", hl.dsp.Move_window_to_workspace_3("movetoworkspace", 3), { drag = true })
-- TODO: manual review on line 63 — no mapping for dispatcher "Move window to workspace 4"
-- hl.bind("SUPER + SHIFT + code:13", hl.dsp.Move_window_to_workspace_4("movetoworkspace", 4), { drag = true })
-- TODO: manual review on line 64 — no mapping for dispatcher "Move window to workspace 5"
-- hl.bind("SUPER + SHIFT + code:14", hl.dsp.Move_window_to_workspace_5("movetoworkspace", 5), { drag = true })
-- TODO: manual review on line 65 — no mapping for dispatcher "Move window to workspace 6"
-- hl.bind("SUPER + SHIFT + code:15", hl.dsp.Move_window_to_workspace_6("movetoworkspace", 6), { drag = true })
-- TODO: manual review on line 66 — no mapping for dispatcher "Move window to workspace 7"
-- hl.bind("SUPER + SHIFT + code:16", hl.dsp.Move_window_to_workspace_7("movetoworkspace", 7), { drag = true })
-- TODO: manual review on line 67 — no mapping for dispatcher "Move window to workspace 8"
-- hl.bind("SUPER + SHIFT + code:17", hl.dsp.Move_window_to_workspace_8("movetoworkspace", 8), { drag = true })
-- TODO: manual review on line 68 — no mapping for dispatcher "Move window to workspace 9"
-- hl.bind("SUPER + SHIFT + code:18", hl.dsp.Move_window_to_workspace_9("movetoworkspace", 9), { drag = true })
-- TODO: manual review on line 69 — no mapping for dispatcher "Move window to workspace 10"
-- hl.bind("SUPER + SHIFT + code:19", hl.dsp.Move_window_to_workspace_10("movetoworkspace", 10), { drag = true })

-- TODO: manual review on line 71 — no mapping for dispatcher "Move window silently to workspace 1"
-- hl.bind("SUPER + SHIFT + ALT + code:10", hl.dsp.Move_window_silently_to_workspace_1("movetoworkspacesilent", 1), { drag = true })
-- TODO: manual review on line 72 — no mapping for dispatcher "Move window silently to workspace 2"
-- hl.bind("SUPER + SHIFT + ALT + code:11", hl.dsp.Move_window_silently_to_workspace_2("movetoworkspacesilent", 2), { drag = true })
-- TODO: manual review on line 73 — no mapping for dispatcher "Move window silently to workspace 3"
-- hl.bind("SUPER + SHIFT + ALT + code:12", hl.dsp.Move_window_silently_to_workspace_3("movetoworkspacesilent", 3), { drag = true })
-- TODO: manual review on line 74 — no mapping for dispatcher "Move window silently to workspace 4"
-- hl.bind("SUPER + SHIFT + ALT + code:13", hl.dsp.Move_window_silently_to_workspace_4("movetoworkspacesilent", 4), { drag = true })
-- TODO: manual review on line 75 — no mapping for dispatcher "Move window silently to workspace 5"
-- hl.bind("SUPER + SHIFT + ALT + code:14", hl.dsp.Move_window_silently_to_workspace_5("movetoworkspacesilent", 5), { drag = true })
-- TODO: manual review on line 76 — no mapping for dispatcher "Move window silently to workspace 6"
-- hl.bind("SUPER + SHIFT + ALT + code:15", hl.dsp.Move_window_silently_to_workspace_6("movetoworkspacesilent", 6), { drag = true })
-- TODO: manual review on line 77 — no mapping for dispatcher "Move window silently to workspace 7"
-- hl.bind("SUPER + SHIFT + ALT + code:16", hl.dsp.Move_window_silently_to_workspace_7("movetoworkspacesilent", 7), { drag = true })
-- TODO: manual review on line 78 — no mapping for dispatcher "Move window silently to workspace 8"
-- hl.bind("SUPER + SHIFT + ALT + code:17", hl.dsp.Move_window_silently_to_workspace_8("movetoworkspacesilent", 8), { drag = true })
-- TODO: manual review on line 79 — no mapping for dispatcher "Move window silently to workspace 9"
-- hl.bind("SUPER + SHIFT + ALT + code:18", hl.dsp.Move_window_silently_to_workspace_9("movetoworkspacesilent", 9), { drag = true })
-- TODO: manual review on line 80 — no mapping for dispatcher "Move window silently to workspace 10"
-- hl.bind("SUPER + SHIFT + ALT + code:19", hl.dsp.Move_window_silently_to_workspace_10("movetoworkspacesilent", 10), { drag = true })

-- ── Scratchpad ─────────────────────────────────────────
-- TODO: manual review on line 83 — no mapping for dispatcher "Toggle scratchpad"
-- hl.bind("SUPER + S", hl.dsp.Toggle_scratchpad("togglespecialworkspace", "scratchpad"), { drag = true })
-- TODO: manual review on line 84 — no mapping for dispatcher "Move window to scratchpad"
-- hl.bind("SUPER + ALT + S", hl.dsp.Move_window_to_scratchpad("movetoworkspacesilent", "special:scratchpad"), { drag = true })

-- ── Workspace Navigation ───────────────────────────────
-- TODO: manual review on line 87 — no mapping for dispatcher "Next workspace"
-- hl.bind("SUPER + TAB", hl.dsp.Next_workspace("workspace", "e+1"), { drag = true })
-- TODO: manual review on line 88 — no mapping for dispatcher "Previous workspace"
-- hl.bind("SUPER + SHIFT + TAB", hl.dsp.Previous_workspace("workspace", "e-1"), { drag = true })
-- TODO: manual review on line 89 — no mapping for dispatcher "Former workspace"
-- hl.bind("SUPER + CTRL + TAB", hl.dsp.Former_workspace("workspace", "previous"), { drag = true })

-- ── Monitor ────────────────────────────────────────────
-- TODO: manual review on line 92 — no mapping for dispatcher "Move workspace to left monitor"
-- hl.bind("SUPER + SHIFT + ALT + LEFT", hl.dsp.Move_workspace_to_left_monitor("movecurrentworkspacetomonitor", "l"), { drag = true })
-- TODO: manual review on line 93 — no mapping for dispatcher "Move workspace to right monitor"
-- hl.bind("SUPER + SHIFT + ALT + RIGHT", hl.dsp.Move_workspace_to_right_monitor("movecurrentworkspacetomonitor", "r"), { drag = true })
-- TODO: manual review on line 94 — no mapping for dispatcher "Move workspace to up monitor"
-- hl.bind("SUPER + SHIFT + ALT + UP", hl.dsp.Move_workspace_to_up_monitor("movecurrentworkspacetomonitor", "u"), { drag = true })
-- TODO: manual review on line 95 — no mapping for dispatcher "Move workspace to down monitor"
-- hl.bind("SUPER + SHIFT + ALT + DOWN", hl.dsp.Move_workspace_to_down_monitor("movecurrentworkspacetomonitor", "d"), { drag = true })

-- ── Swap ───────────────────────────────────────────────
-- TODO: manual review on line 98 — no mapping for dispatcher "Swap window to the left"
-- hl.bind("SUPER + SHIFT + LEFT", hl.dsp.Swap_window_to_the_left("swapwindow", "l"), { drag = true })
-- TODO: manual review on line 99 — no mapping for dispatcher "Swap window to the right"
-- hl.bind("SUPER + SHIFT + RIGHT", hl.dsp.Swap_window_to_the_right("swapwindow", "r"), { drag = true })
-- TODO: manual review on line 100 — no mapping for dispatcher "Swap window up"
-- hl.bind("SUPER + SHIFT + UP", hl.dsp.Swap_window_up("swapwindow", "u"), { drag = true })
-- TODO: manual review on line 101 — no mapping for dispatcher "Swap window down"
-- hl.bind("SUPER + SHIFT + DOWN", hl.dsp.Swap_window_down("swapwindow", "d"), { drag = true })

-- ── Cycle ──────────────────────────────────────────────
-- TODO: manual review on line 104 — no mapping for dispatcher "Cycle to next window"
-- hl.bind("ALT + TAB", hl.dsp.Cycle_to_next_window("cyclenext"), { drag = true })
-- TODO: manual review on line 105 — no mapping for dispatcher "Cycle to prev window"
-- hl.bind("ALT + SHIFT + TAB", hl.dsp.Cycle_to_prev_window("cyclenext", "prev"), { drag = true })
-- TODO: manual review on line 106 — no mapping for dispatcher "Reveal active window on top"
-- hl.bind("ALT + TAB", hl.dsp.Reveal_active_window_on_top("bringactivetotop"), { drag = true })
-- TODO: manual review on line 107 — no mapping for dispatcher "Reveal active window on top"
-- hl.bind("ALT + SHIFT + TAB", hl.dsp.Reveal_active_window_on_top("bringactivetotop"), { drag = true })

-- ── Resize ─────────────────────────────────────────────
-- TODO: manual review on line 110 — no mapping for dispatcher "Expand window left"
-- hl.bind("SUPER + code:20", hl.dsp.Expand_window_left("resizeactive", "-100 0"), { drag = true })
-- TODO: manual review on line 111 — no mapping for dispatcher "Shrink window left"
-- hl.bind("SUPER + code:21", hl.dsp.Shrink_window_left("resizeactive", "100 0"), { drag = true })
-- TODO: manual review on line 112 — no mapping for dispatcher "Shrink window up"
-- hl.bind("SUPER + SHIFT + code:20", hl.dsp.Shrink_window_up("resizeactive", "0 -100"), { drag = true })
-- TODO: manual review on line 113 — no mapping for dispatcher "Expand window down"
-- hl.bind("SUPER + SHIFT + code:21", hl.dsp.Expand_window_down("resizeactive", "0 100"), { drag = true })

-- ── Scroll Workspaces ──────────────────────────────────
-- TODO: manual review on line 116 — no mapping for dispatcher "Scroll active workspace forward"
-- hl.bind("SUPER + mouse_down", hl.dsp.Scroll_active_workspace_forward("workspace", "e+1"), { drag = true })
-- TODO: manual review on line 117 — no mapping for dispatcher "Scroll active workspace backward"
-- hl.bind("SUPER + mouse_up", hl.dsp.Scroll_active_workspace_backward("workspace", "e-1"), { drag = true })

-- ── Mouse ──────────────────────────────────────────────
-- TODO: manual review on line 120 — no mapping for dispatcher "Move window"
-- hl.bind("SUPER + mouse:272", hl.dsp.Move_window("movewindow"), { drag = true })
-- TODO: manual review on line 121 — no mapping for dispatcher "Resize window"
-- hl.bind("SUPER + mouse:273", hl.dsp.Resize_window("resizewindow"), { drag = true })

-- ── Groups ─────────────────────────────────────────────
-- TODO: manual review on line 124 — no mapping for dispatcher "Toggle window grouping"
-- hl.bind("SUPER + G", hl.dsp.Toggle_window_grouping("togglegroup"), { drag = true })
-- TODO: manual review on line 125 — no mapping for dispatcher "Move active window out of group"
-- hl.bind("SUPER + ALT + G", hl.dsp.Move_active_window_out_of_group("moveoutofgroup"), { drag = true })
-- TODO: manual review on line 126 — no mapping for dispatcher "Move window to group on left"
-- hl.bind("SUPER + ALT + LEFT", hl.dsp.Move_window_to_group_on_left("moveintogroup", "l"), { drag = true })
-- TODO: manual review on line 127 — no mapping for dispatcher "Move window to group on right"
-- hl.bind("SUPER + ALT + RIGHT", hl.dsp.Move_window_to_group_on_right("moveintogroup", "r"), { drag = true })
-- TODO: manual review on line 128 — no mapping for dispatcher "Move window to group on top"
-- hl.bind("SUPER + ALT + UP", hl.dsp.Move_window_to_group_on_top("moveintogroup", "u"), { drag = true })
-- TODO: manual review on line 129 — no mapping for dispatcher "Move window to group on bottom"
-- hl.bind("SUPER + ALT + DOWN", hl.dsp.Move_window_to_group_on_bottom("moveintogroup", "d"), { drag = true })
-- TODO: manual review on line 130 — no mapping for dispatcher "Next window in group"
-- hl.bind("SUPER + ALT + TAB", hl.dsp.Next_window_in_group("changegroupactive", "f"), { drag = true })
-- TODO: manual review on line 131 — no mapping for dispatcher "Previous window in group"
-- hl.bind("SUPER + ALT + SHIFT + TAB", hl.dsp.Previous_window_in_group("changegroupactive", "b"), { drag = true })
-- TODO: manual review on line 132 — no mapping for dispatcher "Move grouped window focus left"
-- hl.bind("SUPER + CTRL + LEFT", hl.dsp.Move_grouped_window_focus_left("changegroupactive", "b"), { drag = true })
-- TODO: manual review on line 133 — no mapping for dispatcher "Move grouped window focus right"
-- hl.bind("SUPER + CTRL + RIGHT", hl.dsp.Move_grouped_window_focus_right("changegroupactive", "f"), { drag = true })
-- TODO: manual review on line 134 — no mapping for dispatcher "Next window in group"
-- hl.bind("SUPER + ALT + mouse_down", hl.dsp.Next_window_in_group("changegroupactive", "f"), { drag = true })
-- TODO: manual review on line 135 — no mapping for dispatcher "Previous window in group"
-- hl.bind("SUPER + ALT + mouse_up", hl.dsp.Previous_window_in_group("changegroupactive", "b"), { drag = true })
-- TODO: manual review on line 136 — no mapping for dispatcher "Switch to group window 1"
-- hl.bind("SUPER + ALT + code:10", hl.dsp.Switch_to_group_window_1("changegroupactive", 1), { drag = true })
-- TODO: manual review on line 137 — no mapping for dispatcher "Switch to group window 2"
-- hl.bind("SUPER + ALT + code:11", hl.dsp.Switch_to_group_window_2("changegroupactive", 2), { drag = true })
-- TODO: manual review on line 138 — no mapping for dispatcher "Switch to group window 3"
-- hl.bind("SUPER + ALT + code:12", hl.dsp.Switch_to_group_window_3("changegroupactive", 3), { drag = true })
-- TODO: manual review on line 139 — no mapping for dispatcher "Switch to group window 4"
-- hl.bind("SUPER + ALT + code:13", hl.dsp.Switch_to_group_window_4("changegroupactive", 4), { drag = true })
-- TODO: manual review on line 140 — no mapping for dispatcher "Switch to group window 5"
-- hl.bind("SUPER + ALT + code:14", hl.dsp.Switch_to_group_window_5("changegroupactive", 5), { drag = true })

-- ── Launch ─────────────────────────────────────────────
-- TODO: manual review on line 143 — no mapping for dispatcher "Terminal"
-- hl.bind("SUPER + RETURN", hl.dsp.Terminal("exec", "uwsm-app -- xdg-terminal-exec"), { drag = true })
-- TODO: manual review on line 144 — no mapping for dispatcher "Launch apps"
-- hl.bind("SUPER + SPACE", hl.dsp.Launch_apps("exec", "walker"), { drag = true })
-- TODO: manual review on line 145 — no mapping for dispatcher "System menu"
-- hl.bind("SUPER + ALT + SPACE", hl.dsp.System_menu("exec", "system-menu"), { drag = true })
-- TODO: manual review on line 146 — no mapping for dispatcher "Emoji picker"
-- hl.bind("SUPER + CTRL + E", hl.dsp.Emoji_picker("exec", "walker -m symbols"), { drag = true })
-- TODO: manual review on line 147 — no mapping for dispatcher "Power menu"
-- hl.bind("SUPER + ESCAPE", hl.dsp.Power_menu("exec", "power-menu"), { drag = true })
-- TODO: manual review on line 148 — no mapping for dispatcher "Show key bindings"
-- hl.bind("SUPER + K", hl.dsp.Show_key_bindings("exec", "less ~/.config/hypr/bindings.conf"), { drag = true })

-- ── Appearance ─────────────────────────────────────────
-- TODO: manual review on line 151 — no mapping for dispatcher "Toggle top bar"
-- hl.bind("SUPER + SHIFT + SPACE", hl.dsp.Toggle_top_bar("exec", "toggle-waybar"), { drag = true })
-- TODO: manual review on line 152 — no mapping for dispatcher "Next background in theme"
-- hl.bind("SUPER + CTRL + SPACE", hl.dsp.Next_background_in_theme("exec", "bg-next"), { drag = true })
-- TODO: manual review on line 153 — no mapping for dispatcher "Theme menu"
-- hl.bind("SUPER + SHIFT + CTRL + SPACE", hl.dsp.Theme_menu("exec", "theme-menu"), { drag = true })
-- TODO: manual review on line 154 — no mapping for dispatcher "Toggle window transparency"
-- hl.bind("SUPER + BACKSPACE", hl.dsp.Toggle_window_transparency("exec", "toggle-opaque"), { drag = true })
-- TODO: manual review on line 155 — no mapping for dispatcher "Toggle workspace gaps"
-- hl.bind("SUPER + SHIFT + BACKSPACE", hl.dsp.Toggle_workspace_gaps("exec", "toggle-gaps"), { drag = true })

-- ── Notifications ──────────────────────────────────────
-- TODO: manual review on line 158 — no mapping for dispatcher "Dismiss last notification"
-- hl.bind("SUPER + COMMA", hl.dsp.Dismiss_last_notification("exec", "swaync-client --close-latest"), { drag = true })
-- TODO: manual review on line 159 — no mapping for dispatcher "Dismiss all notifications"
-- hl.bind("SUPER + SHIFT + COMMA", hl.dsp.Dismiss_all_notifications("exec", "swaync-client --close-all"), { drag = true })
-- TODO: manual review on line 160 — no mapping for dispatcher "Toggle control center"
-- hl.bind("SUPER + CTRL + COMMA", hl.dsp.Toggle_control_center("exec", "swaync-client --toggle-panel"), { drag = true })
-- TODO: manual review on line 161 — no mapping for dispatcher "Toggle silencing notifications"
-- hl.bind("SUPER + ALT + COMMA", hl.dsp.Toggle_silencing_notifications("exec", "swaync-client --toggle-dnd"), { drag = true })

-- ── Idle / Night Light ─────────────────────────────────
-- TODO: manual review on line 164 — no mapping for dispatcher "Toggle locking on idle"
-- hl.bind("SUPER + CTRL + I", hl.dsp.Toggle_locking_on_idle("exec", "toggle-idle"), { drag = true })
-- TODO: manual review on line 165 — no mapping for dispatcher "Toggle nightlight"
-- hl.bind("SUPER + CTRL + N", hl.dsp.Toggle_nightlight("exec", "toggle-nightlight"), { drag = true })

-- ── Screenshots ────────────────────────────────────────
-- TODO: manual review on line 168 — no mapping for dispatcher "Screenshot with editing"
-- hl.bind("PRINT", hl.dsp.Screenshot_with_editing("exec", "screenshot smart"), { drag = true })
-- TODO: manual review on line 169 — no mapping for dispatcher "Screenshot to clipboard"
-- hl.bind("SHIFT + PRINT", hl.dsp.Screenshot_to_clipboard("exec", "screenshot smart clipboard"), { drag = true })
-- TODO: manual review on line 170 — no mapping for dispatcher "Region screenshot"
-- hl.bind("ALT + PRINT", hl.dsp.Region_screenshot("exec", "screenshot region"), { drag = true })

-- ── Lock ───────────────────────────────────────────────
-- TODO: manual review on line 173 — no mapping for dispatcher "Lock system"
-- hl.bind("SUPER + CTRL + L", hl.dsp.Lock_system("exec", "hyprlock"), { drag = true })

-- ── Application Shortcuts ──────────────────────────────
-- TODO: manual review on line 176 — no mapping for dispatcher "Terminal at CWD"
-- hl.bind("SUPER + SHIFT + RETURN", hl.dsp.Terminal_at_CWD("exec", "uwsm-app -- xdg-terminal-exec"), { drag = true })
-- TODO: manual review on line 177 — no mapping for dispatcher "File manager"
-- hl.bind("SUPER + SHIFT + F", hl.dsp.File_manager("exec", "uwsm-app -- nautilus --new-window"), { drag = true })
-- TODO: manual review on line 178 — no mapping for dispatcher "Browser"
-- hl.bind("SUPER + SHIFT + B", hl.dsp.Browser("exec", "uwsm-app -- firefox"), { drag = true })
-- TODO: manual review on line 179 — no mapping for dispatcher "Browser (private)"
-- hl.bind("SUPER + SHIFT + ALT + B", hl.dsp.Browser__private_("exec", "uwsm-app -- firefox --private-window"), { drag = true })
-- TODO: manual review on line 180 — no mapping for dispatcher "Music"
-- hl.bind("SUPER + SHIFT + M", hl.dsp.Music("exec", "uwsm-app -- spotify"), { drag = true })
-- TODO: manual review on line 181 — no mapping for dispatcher "Editor"
-- hl.bind("SUPER + SHIFT + N", hl.dsp.Editor("exec", "code"), { drag = true })
-- TODO: manual review on line 182 — no mapping for dispatcher "Screenshot clipboard"
-- hl.bind("SUPER + SHIFT + S", hl.dsp.Screenshot_clipboard("exec", "grim -g \"$(slurp)\" - | wl-copy"), { drag = true })
-- TODO: manual review on line 183 — no mapping for dispatcher "Toggle area video recording"
-- hl.bind("SUPER + SHIFT + L", hl.dsp.Toggle_area_video_recording("exec", "~/.config/hypr/scripts/record-area.sh"), { drag = true })
-- TODO: manual review on line 184 — no mapping for dispatcher "Shutdown"
-- hl.bind("XF86PowerOff", hl.dsp.Shutdown("exec", "systemctl poweroff"), { drag = true })

-- ── Info ───────────────────────────────────────────────
-- TODO: manual review on line 187 — no mapping for dispatcher "Show time"
-- hl.bind("SUPER + CTRL + ALT + T", hl.dsp.Show_time("exec", "notify-send \"    $(date +\"%A %H:%M  —  %d %B W%V %Y\")\""), { drag = true })
-- TODO: manual review on line 188 — no mapping for dispatcher "Show battery remaining"
-- hl.bind("SUPER + CTRL + ALT + B", hl.dsp.Show_battery_remaining("exec", "notify-send \"󰁹    Battery: $(cat /sys/class/power_supply/BAT*/capacity 2>/dev/null || echo 'N/A')%\""), { drag = true })

-- ── System Panels ──────────────────────────────────────
-- TODO: manual review on line 191 — no mapping for dispatcher "Audio controls"
-- hl.bind("SUPER + CTRL + A", hl.dsp.Audio_controls("exec", "pavucontrol"), { drag = true })
-- TODO: manual review on line 192 — no mapping for dispatcher "Bluetooth controls"
-- hl.bind("SUPER + CTRL + B", hl.dsp.Bluetooth_controls("exec", "blueman-manager"), { drag = true })
-- TODO: manual review on line 193 — no mapping for dispatcher "Wifi controls"
-- hl.bind("SUPER + CTRL + W", hl.dsp.Wifi_controls("exec", "nm-connection-editor"), { drag = true })
-- TODO: manual review on line 194 — no mapping for dispatcher "Activity"
-- hl.bind("SUPER + CTRL + T", hl.dsp.Activity("exec", "uwsm-app -- xdg-terminal-exec -- btop"), { drag = true })
